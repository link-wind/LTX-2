# ltx-audio-trainer

Audio-only training toolkit for LTX text-to-audio workflows.

This package is currently bootstrapped from `packages/ltx-trainer` and contains
the reusable training infrastructure that can be shared with an audio-only
trainer. Audio-specific dataset processing, strategies, trainer orchestration,
and validation sampling will be added in the next steps.
